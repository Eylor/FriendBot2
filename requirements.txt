# FriendBot2's own dependencies (both modes).
discord.py>=2.4.0
python-dotenv>=1.0.0

# Chat mode: loading the LoRA adapter on top of the base model.
peft>=0.15

# Both backends also need the torch/transformers stack. Those are NOT listed
# here — install them from the flux repo so the versions match the GPU/CUDA
# setup there:
#
#   cd ../flux && bash setup.sh           # creates flux/.venv with torch etc.
#   source ../flux/.venv/bin/activate
#   pip install -r requirements.txt       # add this file on top
#
# For fine-tuning, additionally:  pip install -r requirements-train.txt
# See README.md for details.
